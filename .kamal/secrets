# Kamal secrets — EXTRACTION COMMANDS, never values (the credentials
# doctrine: nothing plaintext lives in the repo; see the framework's
# credentials guide for the store itself).
#
# Examples — pick your source and delete the rest:
#   KAMAL_REGISTRY_PASSWORD=$(op read "op://vault/registry/password")
#   LIGHTYEAR_MASTER_KEY=$(op read "op://vault/as_of/master-key")
# or from your environment at deploy time:
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
LIGHTYEAR_MASTER_KEY=$LIGHTYEAR_MASTER_KEY

# TRANSITIONAL (dies at gold master): the private framework gems need a
# read token at IMAGE BUILD (Contents: Read-only on the framework repo):
LIGHTYEAR_GEM_READ_TOKEN=$LIGHTYEAR_GEM_READ_TOKEN
