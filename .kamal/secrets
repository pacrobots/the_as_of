# Extraction commands only — never raw secrets.
KAMAL_REGISTRY_PASSWORD=$(gh auth token -u walkaboutlanguages)
LIGHTYEAR_GEM_READ_TOKEN=$(gh auth token -u pacrobots)
lightyear_gem_read_token=$(gh auth token -u pacrobots)
LIGHTYEAR_MASTER_KEY=$(cat config/master.key)
SITE_PASSWORD=$(cat .kamal/site_password)
# Pull from the local Realm store at deploy time; value never lives in git.
FRED_API_KEY=$(bundle exec ruby -e 'require "./config/lightyear"; print Lightyear::Trust::Credentials.resolve(realm: Lightyear.realm, key: :fred_api_key).to_s')
