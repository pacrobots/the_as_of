# CLAUDE.md

See **AGENTS.md** — the standing instructions for every coding agent on this
app, yourself included. The short version: do the walkaround (`lightyear
walkaround`), pass the checkride (`lightyear checkride`), paste the report
card to the developer, and only then take the controls.
